name: datamend_demo
version: "0.1.0"
config-version: 2
profile: datamend

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  datamend_demo:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
